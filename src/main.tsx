import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App'; // ඔබේ App.jsx ගොනුව මෙතැනට සම්බන්ධ කරයි

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
